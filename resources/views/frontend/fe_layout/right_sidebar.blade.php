
@if ($data)    
{{-- <div class="widget-wrap">
    <div class="single-sidebar-widget popular-post-widget">
        <h4 class="popular-title">Waiting List</h4>
        <div class="popular-post-list">
            @foreach ($data->waiting as $item)
               <p>{{$item->user->name}}</p> 
            @endforeach
        </div>
    </div>
</div> --}}
@endif
